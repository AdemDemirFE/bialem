import { Stack } from "expo-router";import { useEffect,useState } from "react";import { ScrollView,StyleSheet,Text,View } from "react-native";import { Reveal, Skeleton } from "../../src/animations";import { managementApi,type ManagementDashboard } from "../../src/lib/management-api";import { colors } from "../../src/theme/colors";
export default function ManagementData(){const[d,setD]=useState<ManagementDashboard|null>(null);useEffect(()=>{void managementApi.dashboard().then(setD)},[]);return <><Stack.Screen options={{headerShown:true,title:"Veriler"}}/><ScrollView style={s.screen} contentContainerStyle={s.page}><Reveal><Text style={s.title}>Platform Verileri</Text></Reveal>{!d?<View style={{gap:10}}><Skeleton height={76} borderRadius={18}/><Skeleton height={76} borderRadius={18}/><Skeleton height={76} borderRadius={18}/></View>:Object.entries({"Toplam kullanıcı":d.users.total,"Toplam topluluk":d.communities.total,"Toplam etkinlik":d.events.total,"Toplam bildirim":d.communications.notificationsSent,"Açık rapor":d.moderation.openReports}).map(([k,v],i)=><Reveal key={k} index={Math.min(i,4)}><View style={s.card}><Text style={s.value}>{v}</Text><Text style={s.label}>{k}</Text></View></Reveal>)}</ScrollView></>};const s=StyleSheet.create({screen:{flex:1,backgroundColor:colors.page},page:{padding:16,gap:10},title:{fontSize:27,fontWeight:"900",color:colors.ink},card:{padding:16,borderRadius:18,backgroundColor:colors.surface,borderWidth:1,borderColor:colors.border},value:{fontSize:25,fontWeight:"900",color:colors.ink},label:{fontSize:12,color:colors.muted}});
