import { Ionicons } from "@expo/vector-icons"; import { ActivityIndicator, StyleSheet, Text, View } from "react-native";
import { colors } from "../../theme/colors"; import { radius, spacing, typography } from "../../theme/tokens"; import { AppButton } from "./AppButton";
export function FeedbackState({kind,title,message,onRetry}:{kind:"loading"|"empty"|"error";title?:string;message?:string;onRetry?:()=>void}){return <View accessibilityRole={kind==="error"?"alert":undefined} style={s.box}>{kind==="loading"?<ActivityIndicator color={colors.accent}/>:<Ionicons name={kind==="error"?"cloud-offline-outline":"file-tray-outline"} size={32} color={kind==="error"?colors.danger:colors.accent}/>} {title?<Text style={s.title}>{title}</Text>:null}{message?<Text style={s.message}>{message}</Text>:null}{onRetry?<AppButton label="Tekrar dene" variant="secondary" size="small" onPress={onRetry}/>:null}</View>}
const s=StyleSheet.create({box:{minHeight:180,alignItems:"center",justifyContent:"center",gap:spacing.sm,padding:spacing.xxl,borderRadius:radius.xl,borderWidth:1,borderColor:colors.border,backgroundColor:colors.surface},title:{...typography.sectionTitle,color:colors.ink,textAlign:"center"},message:{...typography.secondary,color:colors.muted,textAlign:"center"}});
