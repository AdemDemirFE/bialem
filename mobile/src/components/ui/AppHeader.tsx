import { StyleSheet, Text, View, type ReactNode } from "react-native"; import { BackButton } from "../IconButton"; import { colors } from "../../theme/colors"; import { sizes, spacing, typography } from "../../theme/tokens";
export function AppHeader({title,onBack,action}:{title:string;onBack?:()=>void;action?:ReactNode}){return <View style={s.header}>{onBack?<BackButton onPress={onBack}/>:<View style={s.slot}/>}<Text numberOfLines={1} style={s.title}>{title}</Text><View style={s.slot}>{action}</View></View>}
const s=StyleSheet.create({header:{minHeight:sizes.header,flexDirection:"row",alignItems:"center",gap:spacing.sm,paddingHorizontal:spacing.md,backgroundColor:colors.surface,borderBottomWidth:1,borderBottomColor:colors.border},slot:{width:sizes.touch,minHeight:sizes.touch,alignItems:"center",justifyContent:"center"},title:{...typography.cardTitle,flex:1,color:colors.ink,textAlign:"center"}});
